= @board.invite.name
= t(@board.created_at)

%ul.list-group
  - @board.answers.each do |answer|
    %li.list-group-item
      .question
        %strong= answer.question.title
      .answer= answer.content

= link_to '戻る', answer_boards_path
